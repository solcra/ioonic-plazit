import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rolaps.plazit',
  appName: 'platzi-music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
